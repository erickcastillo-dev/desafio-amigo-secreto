// variables
let nombres = [];

function agregarAmigos() {
    document.getElementById(amigo);
    if(agregarAmigos == '') {
        alert 'Por favor, inserte su nombre';
    } else {
        nombres.push(agregarAmigos);
        )
    }
}

// Validar entrada, (si el campo está vacio retonar un "alert", si no agregar el avlor al array.) 
// ¿Como? lo averiguaremos 